{
  "name": "marketmind-proxy",
  "version": "1.0.0",
  "description": "Multi-model pipeline proxy for MarketMind Pro — Perplexity → DeepSeek → Claude",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
